 <Grid Background="NavajoWhite">
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="50" />
        </Grid.RowDefinitions>
        <ListBox x:Name="profList" ItemsSource="{Binding}" Background="WhiteSmoke">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel>
                        <TextBlock Text="{Binding Profession ID}" FontSize="16" />
                        <TextBlock Text="{Binding Profession Name}" FontSize="12" />
                        <TextBlock Text="{Binding Multiple Occupation}" FontSize="13" />
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center">
            <Button  Foreground="DarkSlateGray" FontSize="17" FontWeight="Medium" Content="Добавить" Margin="10" Click="Add_Click" />
            <Button  Foreground="DarkSlateGray" FontSize="17" FontWeight="Medium" Content="Изменить" Margin="10" Click="Edit_Click"  />
            <Button  Foreground="DarkSlateGray" FontSize="17" FontWeight="Medium" Content="Удалить" Margin="10" Click="Delete_Click"  />
           
        </StackPanel>
    </Grid>